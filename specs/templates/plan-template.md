# Plan: Spec NNN — <Title>

**Spec:** [spec.md](spec.md)
**Status:** draft | approved

## Technical approach

<How the requirements will be met: architecture, data flow, key decisions with rationale.>

## Stack and versions

<Exact versions of every tool this spec touches, verified on the machine at planning time (`nargo --version`, `bb --version`, etc.). If a version differs from the table in `CLAUDE.md`, reconcile before proceeding.>

## Design

<File layout to be created/modified, interfaces, data schemas (e.g., benchmark JSON schema), CLI entry points.>

## Risks and mitigations

| Risk | Impact | Mitigation |
|------|--------|------------|
| <e.g., bb/nargo API drift vs. docs> | <impact> | <check installed version docs first; smoke-test on minimal example> |

## Verification plan

<How to prove the acceptance criteria are met end-to-end: commands to run, expected outputs, manual checks.>
