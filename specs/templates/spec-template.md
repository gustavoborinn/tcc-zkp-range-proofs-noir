# Spec NNN: <Title>

**Branch:** `spec/NNN-short-name`
**Status:** draft | approved | in progress | done
**Depends on:** <spec numbers or "none">

## Overview

<2–4 sentences: what this spec delivers and why it exists in the context of the thesis.>

## Methodology traceability

<Which sections of `docs/methodology.md` (and, where relevant, `docs/expected_results.md`) this spec implements. Quote the fixed protocol parameters that constrain this work (e.g., N=100, K=15, alpha=0.05). This section is the contract that keeps the implementation scientifically valid.>

## Functional requirements

- FR-1: <requirement>
- FR-2: <requirement>

## Out of scope

- <Explicitly excluded work, especially things that belong to other specs.>

## Acceptance criteria

Each criterion must be objectively checkable.

- AC-1: <e.g., "`nargo test` passes in all four circuit packages, including negative tests">
- AC-2: <e.g., "benchmark run produces `results/native/<condition>.json` with exactly 100 samples per condition">

## Open questions

- <Decisions deferred to plan.md, with the information needed to resolve them.>
